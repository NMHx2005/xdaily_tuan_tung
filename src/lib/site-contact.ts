/**
 * Re-export đồng bộ từ `site-content` (mặc định code).
 * Header/Footer trên site dùng dữ liệu từ `getMergedSiteContent()` qua props.
 */
export {
  SITE_HOTLINE,
  SITE_HOTLINE_TEL,
  SITE_HOTLINE_DISPLAY,
  SITE_CONTACT_EMAIL,
  SITE_ADDRESS,
  SITE_OPENING_HOURS,
  getGoogleMapsSearchUrl,
  siteBrand,
  siteContact,
} from "@/content/site-content";
